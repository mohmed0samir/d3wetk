/* RSVP is deliberately removed, rather than merely hidden, from the published invitation. */
document.addEventListener('DOMContentLoaded',()=>{document.querySelector('#rsvpButton')?.remove();document.querySelector('#rsvpModal')?.remove()});
